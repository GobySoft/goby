protobuf_generate_cpp(PROTO_SRCS PROTO_HDRS two_message.proto)
include_directories(${CMAKE_CURRENT_BINARY_DIR})
add_executable(two_message two_message.cpp ${PROTO_SRCS} ${PROTO_HDRS})
target_link_libraries(two_message ${GOBY_LIBRARIES} ${Boost_LIBRARIES} ${PROTOBUF_LIBRARIES})
