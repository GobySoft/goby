import "goby/protobuf/option_extensions.proto";

message BytesMsg
{
  option (goby.msg).dccl.id = 10;
  option (goby.msg).dccl.max_bytes = 32;

  required bytes req_bytes = 1 [(goby.field).dccl.max_length=8];
  optional bytes opt_bytes = 2 [(goby.field).dccl.max_length=8];  
}
