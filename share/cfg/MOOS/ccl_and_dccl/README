to run:

(virtual serial ports)
(> sudo apt-get install socat)
> socat -d -d -v pty,raw,echo=0,link=/tmp/ttyLOOPA1 pty,raw,echo=0,link=/tmp/ttyLOOPA2
> socat -d -d -v pty,raw,echo=0,link=/tmp/ttyLOOPB1 pty,raw,echo=0,link=/tmp/ttyLOOPB2


(probably other windows / workspaces)
> pAntler mm1.moos
> pAntler mm2.moos


this example demonstrates both CCL (pREMUSCodec + pAcommsHandler) and DCCL functionality (pAcommsHandler)
mm1 sends DCCL "SIMPLE_STATUS" messages to mm2
mm2 sends CCL "REMUS_STATE" messages to mm1