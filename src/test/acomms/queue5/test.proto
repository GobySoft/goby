import "goby/common/protobuf/option_extensions.proto";

message GobyMessage
{
  option (goby.msg).dccl.id = 4;
  option (goby.msg).dccl.max_bytes = 32;

  // one byte
  required int32 telegram = 1 [(goby.field).dccl.min=0,
                               (goby.field).dccl.max=255];
}

