This example demonstrates two vehicles exchanging position status information.

1. To generate the basic example:

> ./generate.sh

To run:

(probably other windows / workspaces)
> pAntler mm1.moos
> pAntler mm2.moos

2. To generate the example using the Micro-Modem driver instead of the UDP Driver, use

> ./generate.sh mmdriver

