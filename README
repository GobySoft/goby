~~~~~~~~~~~~~~
goby project
~~~~~~~~~~~~~~
see http://launchpad.net/goby for more information

~~~~~~~~~~~~~~
compiling goby
~~~~~~~~~~~~~~

satisfy the third party dependencies in DEPENDENCIES:
> ./DEPENDENCIES
run cmake to configure and generate Makefiles
> ./CONFIGURE
or to build with MOOS applications
> ./CONFIGURE -Dbuild_moos=ON
then, to install locally (goby/bin, goby/include, goby/lib
> ./INSTALL
to install to system
> sudo ./INSTALL

